#include <stdio.h>

int main() {
    int a = 5, b = 10;    // ',' separates variables, ';' ends statement
    if(a < b) {            // '{}' defines block of code
        printf("a is less than b\n");
    }
    return 0;
}

